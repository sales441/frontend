import {useState} from 'react'
const API_BASE=import.meta.env.VITE_API_BASE_URL||''
export default function AI(){const [q,setQ]=useState('Suggest top 3 actions to reduce ACOS this week.');const [out,setOut]=useState(null);const [loading,setLoading]=useState(false);const [err,setErr]=useState('');const ask=async()=>{setLoading(true);setErr('');setOut(null);try{const r=await fetch(`${API_BASE}/ia/suggest`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({prompt:q})});if(!r.ok)throw new Error('AI error: '+r.status);setOut(await r.json())}catch(e){setErr(e.message)}finally{setLoading(false)}};return(<div><h2>AI</h2><p>Ask the AI for suggestions or analysis about your ads:</p><div style={{display:'flex',gap:8,margin:'12px 0'}}><input className='input' value={q} onChange={e=>setQ(e.target.value)} placeholder='Type your question'/><button className='btn' onClick={ask}>Ask AI</button></div>{loading&&<p>Thinking…</p>}{err&&<p style={{color:'crimson'}}>Error: {err}</p>}{out&&(<div className='kpi'><strong>AI Response</strong><pre style={{whiteSpace:'pre-wrap'}}>{JSON.stringify(out,null,2)}</pre></div>)}</div>)}
